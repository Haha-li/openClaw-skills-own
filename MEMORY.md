# MEMORY.md

## 用户偏好（长期）
- 天气查询默认使用 `weather` skill（wttr.in）。
- 网页搜索默认使用 `ddg-web-search`。
- 天气输出模板：`当前 + 未来3天 + 体感建议`。
- 默认不启用“短答模式”（用户明确说过“不用”）。
- 用户提到“浏览器/访问网站”时，优先使用本机已配置的无头浏览器自动化链路（agent-browser + CDP 本地容器），而不是先让用户手动操作。
- 在微信通道里需要发截图时，默认改发“链接截图服务 URL”，不优先走图片直传。

## 已确认流程
- 用户说“git提交”时：按 `skills/git-submit-scripts-skills/SKILL.md` 执行（更新 README、提交并推送 `scripts/ skills/ README.md`）。
- 每次安装新 skill 前，先使用 `skill-vetter` 做安全审查；审查后必须等待用户确认，再执行安装。

## 禁止/停用项
- 淘宝返利自动转链功能已按用户要求停用（不再默认执行）。

## ClawHub已学技能（免Key）
- Skill 名称：n2-free-search
  - 功能：通过 SearXNG 提供免费、免 API Key 的多引擎网页/新闻/图片/视频搜索能力。
  - 典型使用场景：
    - 需要免 Key 的通用网页检索（替代付费搜索 API）。
    - 做时效性信息检索（新闻）或素材收集（图片/视频）。
    - 希望同一套工具覆盖多搜索源并减少供应商绑定。
  - 推荐触发关键词：`免Key搜索`、`SearXNG`、`多引擎搜索`、`搜新闻/图片/视频`、`免费搜索`。

- Skill 名称：ddg-web-search（技能内 name: ddg-search）
  - 功能：通过 DuckDuckGo Lite + `web_fetch` 实现免 API Key 的网页搜索。
  - 典型使用场景：
    - `web_search` 不可用或报 missing_brave_api_key 时兜底搜网。
    - 临时做资料检索、找官网/文档链接。
    - 需要快速拿到标题+摘要+URL 的轻量研究任务。
  - 推荐触发关键词：`免key搜索`、`DuckDuckGo`、`DDG`、`没配Brave key`、`兜底搜一下`。

- Skill 名称：weather
  - 功能：基于 wttr.in（主）与 Open‑Meteo（备）提供实时天气与天气预报，免 API Key。
  - 典型使用场景：
    - 出门前快速看当前天气、体感相关信息（温度/风速/湿度）。
    - 行程前查看某城市当日或近几天预报。
    - 需要脚本化获取天气 JSON 时用 Open‑Meteo 兜底。
  - 推荐触发关键词：`查天气`、`天气预报`、`wttr.in`、`免Key天气`、`出门穿什么`。

- Skill 名称：video-frames
  - 功能：用 ffmpeg 从视频中抽帧或截取短片段，快速拿到关键画面。
  - 典型使用场景：
    - 从长视频里快速截某一时刻画面用于汇报/复盘。
    - 给视频生成预览图或封面候选图。
    - 排查“某个时间点到底发生了什么”这类问题。
  - 推荐触发关键词：`视频抽帧`、`截一帧`、`ffmpeg`、`视频取证`、`做封面图`。

- Skill 名称：tmux
  - 功能：远程控制 tmux 会话，向交互式 CLI 发送按键并抓取终端输出。
  - 典型使用场景：
    - 需要自动化操控必须交互的终端程序（REPL、CLI 向导）。
    - 并行跑多个编码代理/任务并分别查看进度。
    - 在后台长时间运行会话，随时回看 pane 输出定位问题。
  - 推荐触发关键词：`tmux`、`交互式终端自动化`、`发送按键`、`抓取pane输出`、`并行跑CLI`。

- Skill 名称：agent-browser
  - 功能：通过本地 headless 浏览器 CLI 做网页导航、点击、填表、抓取与截图自动化。
  - 典型使用场景：
    - 自动化执行网页流程（登录、表单提交、后台操作回归）。
    - 结构化抓取页面信息（元素文本、属性、数量）。
    - 需要生成页面截图/PDF 或录制网页操作片段。
    - 调试前端交互问题（console/errors/trace）。
  - 推荐触发关键词：`网页自动化`、`浏览器自动操作`、`自动填表`、`页面截图`、`抓取页面元素`。

- Skill 名称：openclaw-healthcheck
  - 功能：对 OpenClaw 部署做轻量运维与安全体检（进程/端口/配置风险/日志异常/更新卫生）。
  - 典型使用场景：
    - 上线前做一次 OpenClaw 部署安全与暴露面检查。
    - 怀疑网关配置不安全或服务“亚健康”时快速排查。
    - 在服务器/树莓派/工作站上做周期性健康巡检。
  - 推荐触发关键词：`OpenClaw健康检查`、`安全审计`、`暴露面检查`、`生产前巡检`、`运行状态体检`。

- Skill 名称：ffmpeg-cli
  - 功能：基于本地 FFmpeg 命令行脚本，一站式完成视频/音频转码、裁剪、合并、抽音频、缩略图/GIF、加水印等处理。
  - 典型使用场景：
    - 需要快速把视频转成目标格式（如 avi→mp4）。
    - 从长视频中裁剪片段、合并多个片段做成成片。
    - 从视频提取音频、做封面图或生成短 GIF。
    - 需要批量做基础视频处理但不想手写 ffmpeg 参数。
  - 推荐触发关键词：`ffmpeg处理`、`视频转码`、`视频裁剪`、`抽取音频`、`做GIF`、`加水印`。

- Skill 名称：pdf
  - 功能：本地 PDF 工具包，可做文本/表格提取、合并拆分、生成 PDF、表单处理等。
  - 典型使用场景：
    - 批量抽取 PDF 文本或表格做整理分析。
    - 合并多个 PDF、按页拆分、旋转页面。
    - 生成报告 PDF 或给现有文档加水印/加密。
    - 处理/填写 PDF 表单自动化流程。
  - 推荐触发关键词：`PDF处理`、`提取PDF文本`、`PDF表格提取`、`合并PDF`、`拆分PDF`、`填写PDF表单`。

- Skill 名称：pdf-extract
  - 功能：使用本地 `pdftotext` 将 PDF 内容提取为纯文本，便于检索与喂给 LLM。
  - 典型使用场景：
    - 先把长 PDF（合同/论文/报告）转成文本再做总结与问答。
    - 只提取指定页码（如 1-5 页）进行快速审阅。
    - 构建“PDF → 文本 → 自动分析”的批处理流程。
  - 推荐触发关键词：`PDF转文本`、`提取PDF文字`、`pdftotext`、`按页提取PDF`、`PDF喂给LLM`。

- Skill 名称：free-local-web-search
  - 功能：在本机部署 localhost-only 的 SearXNG，实现免 API Key 的免费网页搜索。
  - 典型使用场景：
    - 未配置或不想使用任何搜索 API Key 时做日常网页检索。
    - 需要“本地可控、低外部依赖”的搜索兜底方案。
    - 对隐私/可控性要求较高，希望搜索服务仅绑定本机回环地址。
  - 推荐触发关键词：`免Key搜索`、`本地搜索`、`SearXNG`、`免费搜网`、`localhost搜索`。

- Skill 名称：ffmpeg-video-editor
  - 功能：把自然语言视频编辑需求翻译为可直接执行的 FFmpeg 命令（裁剪、转码、压缩、改比例、抽音频等）。
  - 典型使用场景：
    - 用户只会描述“我要怎么改视频”，但不想手写 ffmpeg 参数。
    - 快速生成常见处理命令（截取片段、改分辨率、转 GIF、加水印）。
    - 做批量视频处理前，先生成标准命令模板再执行。
  - 推荐触发关键词：`ffmpeg命令生成`、`自然语言改视频`、`视频裁剪转码`、`转GIF`、`加水印`。

- Skill 名称：openmeteo-sh-weather-simple
  - 功能：基于 openmeteo-sh CLI 调用 Open‑Meteo 免费天气接口，查询实时天气与最多16天天气预报（免 API Key）。
  - 典型使用场景：
    - 未配置任何天气 Key，但需要快速查某城市当前天气。
    - 出行前看未来几天温度、降水概率、风力趋势。
    - 需要按经纬度查天气（如旅行地点、户外点位）。
    - 对“明天/周末会不会下雨”做快速判断。
  - 推荐触发关键词：`查天气`、`OpenMeteo`、`免Key天气`、`明天会下雨吗`、`按经纬度天气`。

- Skill 名称：duckduckgo-search
  - 功能：通过 DuckDuckGo 搜索（文本/新闻/图片/视频等）获取实时网络信息。
  - 典型使用场景：
    - 需要免 Key 的通用网页检索（当前资讯、教程、官方文档）。
    - 想按时间范围查新闻（如过去24小时/一周）。
    - 做素材收集时同时搜图片或视频来源。
    - 需要隐私友好的搜索来源作为默认或备选。
  - 推荐触发关键词：`DuckDuckGo搜索`、`免Key搜网`、`查实时资讯`、`搜新闻`、`搜图片/视频`。

- Skill 名称：skill-vetter
  - 功能：在安装任何新 skill 前做安全审查，识别风险权限与可疑行为，先给出是否安全的审查结论。
  - 典型使用场景：
    - 准备从 ClawHub/GitHub 安装新 skill 前先做一次安全体检。
    - 拿到来源不明的 skill，先判断是否存在数据外传或越权风险。
    - 团队协作时统一输出“可装/谨慎/禁止安装”的审查报告。
  - 推荐触发关键词：`安装前审查`、`skill安全检查`、`vet一下这个skill`、`风险评估`、`先别装先审`。

- Skill 名称：node-connect
  - 功能：诊断 OpenClaw 节点在 Android/iOS/macOS 端的连接与配对失败问题（QR/手动连接/Tailscale/公网路由）。
  - 典型使用场景：
    - 扫码或手动连接时报 `pairing required`、`unauthorized`、`bootstrap token invalid or expired`。
    - 局域网可连但远程（VPS/Tailnet）不可连，需要定位网关实际暴露路由。
    - 怀疑 `gateway.bind`、`gateway.remote.url`、`publicUrl` 配置不一致导致连不上。
  - 推荐触发关键词：`节点连不上`、`OpenClaw配对失败`、`QR连接失败`、`Tailscale连不上`、`gateway.bind`。

- Skill 名称：weathercli
  - 功能：调用免费 Open‑Meteo 数据源，提供全球地点的实时天气与逐日/逐小时预报（免 API Key）。
  - 典型使用场景：
    - 临时查询某城市当前温度、体感、降水和风力。
    - 出行前看未来 3-7 天天气趋势与降雨概率。
    - 需要逐小时天气（如活动/拍摄/户外安排）时做细粒度判断。
    - 需要结构化 JSON 天气数据做后续自动化处理。
  - 推荐触发关键词：`查天气`、`逐小时预报`、`Open-Meteo`、`免Key天气`、`天气JSON`。

- Skill 名称：weather-pollen
  - 功能：基于免费 API 提供“天气 + 花粉”联合报告，免 API Key。
  - 典型使用场景：
    - 春季/过敏季想同时看天气与花粉等级，安排外出。
    - 需要给家人做“今日是否适合户外活动”的快速判断。
    - 关注某地未来天气时，顺带评估鼻炎/过敏风险。
  - 推荐触发关键词：`花粉预报`、`天气+花粉`、`过敏指数`、`今天适合外出吗`、`免Key天气`。

- Skill 名称：openmeteo-sh-weather-advanced
  - 功能：基于 Open-Meteo 免费接口（通过 openmeteo-sh CLI）进行进阶天气查询，支持历史天气、精细变量和模型选择。
  - 典型使用场景：
    - 需要查询历史天气（如某月降雨、某天温度）做复盘或报告。
    - 需要细粒度变量（气压、露点、雪深、逐小时降水概率）进行分析。
    - 需要指定天气模型或控制 forecast 时间窗做更精确查询。
  - 推荐触发关键词：`历史天气`、`OpenMeteo进阶`、`逐小时降水概率`、`天气模型`、`精细天气变量`。

- Skill 名称：ocr-local
  - 功能：使用 Tesseract.js 在本地离线识别图片文字（中英支持），无需 API Key。
  - 典型使用场景：
    - 从截图中提取文字（聊天截图、报错截图、界面文案）。
    - 扫描件/照片转可检索文本，便于总结和问答。
    - 混合中英文图片做快速 OCR 提取。
  - 推荐触发关键词：`图片转文字`、`OCR`、`截图提取文字`、`离线OCR`、`中英识别`。

- Skill 名称：context-compactor
  - 功能：为本地模型会话做“基于 token 估算”的自动上下文压缩，防止长对话超上下文后崩坏。
  - 典型使用场景：
    - 使用 MLX / llama.cpp / Ollama 等本地模型时，长对话容易静默截断或输出异常。
    - 希望在触顶前自动总结旧消息，保留近期关键上下文。
    - 需要查看当前上下文占用并手动触发压缩（如 `/context-stats`、`/compact-now`）。
  - 推荐触发关键词：`本地模型上下文压缩`、`context overflow`、`长对话变乱`、`context-compactor`、`/context-stats`。

- Skill 名称：wiki-local
  - 功能：在本地构建可搜索、可互链的个人知识 Wiki，并支持对话式增删改查。
  - 典型使用场景：
    - 把零散笔记沉淀成结构化知识库（概念、术语、经验总结）。
    - 做主题学习时记录条目并自动建立关联（含反向链接）。
    - 需要快速全文检索某个知识点并查看相关文章。
    - 定期导出静态 wiki/markdown/PDF 做归档或分享。
  - 推荐触发关键词：`本地知识库`、`个人wiki`、`知识管理`、`全文检索笔记`、`双向链接`。

- Skill 名称：file-deduplicator
  - 功能：本地扫描并识别重复文件，支持预览后删除/归档，帮助回收磁盘空间。
  - 典型使用场景：
    - 清理下载目录、文档目录里重复保存的文件。
    - 照片/视频/素材多版本堆积后做空间瘦身。
    - 项目目录排查重复构建产物或冗余副本。
  - 推荐触发关键词：`重复文件清理`、`文件去重`、`磁盘空间不够`、`目录瘦身`、`deduplicate`。

- Skill 名称：recall-local
  - 功能：在本机启动轻量记忆检索服务，索引历史 memory 文件并提供关键词搜索（含网页 UI + 本地 API）。
  - 典型使用场景：
    - 想快速找回“之前某天聊过/做过什么”。
    - 需要在大量日记式 memory 文件里按关键词定位决策与结论。
    - 回顾 bug 修复、流程约定、项目演进时间线。
    - 通过本地网页界面给人类直接检索历史记录。
  - 推荐触发关键词：`回忆一下之前`、`本地记忆检索`、`搜历史记录`、`recall-local`、`memory搜索`。

- Skill 名称：local-memory
  - 功能：用 ChromaDB + BGE-small-zh 在本地做语义记忆的存储、检索与删除，作为 memory 的离线替代方案。
  - 典型使用场景：
    - 想把“重要偏好/决策/事实”结构化写入长期记忆。
    - 需要按语义而非关键词回忆历史信息（相近意思也能召回）。
    - 想在本地环境里做可控的记忆增删查，不依赖云端服务。
  - 推荐触发关键词：`记住这件事`、`语义回忆`、`本地记忆库`、`memory recall`、`forget`。

- Skill 名称：ocr-local-v2（技能内 name: ocr-local）
  - 功能：基于 Tesseract.js 在本地离线做图片文字识别（中简/中繁/英文），无需 API Key。
  - 典型使用场景：
    - 从聊天截图、报错截图中快速提取文字用于总结或排错。
    - 把扫描件/照片中的文字转成可搜索文本。
    - 处理中英混合图片内容，做结构化整理前的文本提取。
  - 推荐触发关键词：`OCR`、`图片转文字`、`截图提取文字`、`离线OCR`、`中英识别`。

- Skill 名称：local-whisper
  - 功能：基于 OpenAI Whisper 在本地离线进行语音转文字（STT），支持多模型与时间戳输出。
  - 典型使用场景：
    - 把会议录音/语音备忘快速转成可检索文本。
    - 从视频或音频素材中提取字幕初稿。
    - 需要离线转写敏感语音内容，避免上传云端。
    - 为后续总结、问答、知识入库提供原始转写文本。
  - 推荐触发关键词：`语音转文字`、`Whisper`、`离线转写`、`会议录音转文本`、`加时间戳转写`。

- Skill 名称：local-whisper-cpp
  - 功能：基于 whisper.cpp 的 `whisper-cli` 在本地离线完成语音转文字（STT），无需云端。
  - 典型使用场景：
    - 服务器/本机批量转写音频文件（会议录音、访谈、播客）。
    - 在不方便联网或对隐私要求高的环境做离线转写。
    - 已有 whisper.cpp 环境时，快速用脚本统一转写流程。
  - 推荐触发关键词：`whisper.cpp`、`离线语音转文字`、`本地转写`、`whisper-cli`、`音频转文本`。

- Skill 名称：speech-recognition-local
  - 功能：基于 faster-whisper 在本地离线完成语音转文字（STT），免 API Key。
  - 典型使用场景：
    - 收到 `.ogg/.m4a/.mp3/.wav` 语音后快速转文字做总结或检索。
    - 需要低成本、可离线的语音转写流程（会议语音、语音备忘）。
    - 对隐私敏感，不希望把语音上传第三方云转写服务。
  - 推荐触发关键词：`本地语音转文字`、`免Key转写`、`faster-whisper`、`离线STT`、`语音转文本`。

- Skill 名称：neverforget
  - 功能：将 OpenClaw 记忆检索切到本地向量记忆栈（node-llama-cpp + 本地 embedding 模型），并附带健康检查与循环索引防护。
  - 典型使用场景：
    - 想把记忆检索改为本地离线方案，减少外部依赖。
    - 需要在本机维护“可持续增长”的长期记忆库，并定期体检状态。
    - 担心索引把自身数据库反复吃进去，想启用排除规则防止递归循环。
  - 推荐触发关键词：`本地记忆检索`、`离线向量记忆`、`memory-core`、`node-llama-cpp`、`防递归索引`。

- Skill 名称：device-heartbeat
  - 功能：通过 healthchecks.io 心跳机制监控远程设备在线状态，并在掉线时触发告警。
  - 典型使用场景：
    - 远程 Mac/主机偶发离线，想持续监控“是否还在线”。
    - 用户反馈设备无响应，需要快速判断是设备宕机还是网络问题。
    - 给多台设备做低成本在线巡检（每台设备一个 heartbeat）。
  - 推荐触发关键词：`设备在线监控`、`心跳检测`、`设备掉线告警`、`is my device online`、`healthchecks`。

- Skill 名称：screenshot-ocr-tool
  - 功能：从屏幕截图中本地提取文字，并可输出纯文本/结构化/问答分离三种格式。
  - 典型使用场景：
    - 从聊天截图或题目截图快速抽取可复制文本。
    - 识别考试题截图并按“题目+选项+答案”结构整理。
    - 将图片内容转成问答对，便于后续总结或喂给模型。
  - 推荐触发关键词：`截图OCR`、`图片转文字`、`题目截图识别`、`问答分离OCR`、`本地OCR`。

- Skill 名称：exa-web-search-free
  - 功能：通过 Exa MCP 提供免 API Key 的网页搜索、代码上下文搜索与公司研究能力。
  - 典型使用场景：
    - 需要实时搜网（新闻、资料、事实核查）但不想配搜索 Key。
    - 需要从 GitHub/Stack Overflow 定位代码示例与文档片段。
    - 需要快速做公司背景与动态信息检索。
  - 推荐触发关键词：`免Key搜网`、`Exa搜索`、`代码示例检索`、`公司研究`、`MCP搜索`。

- Skill 名称：udp-messenger（LocalUDPMessenger）
  - 功能：让同一局域网内的 OpenClaw 代理通过 UDP 互发消息、发现节点和查看消息日志。
  - 典型使用场景：
    - 多个本地代理需要协作分工时，做点对点消息协调。
    - 在内网里发现其他在线代理并建立受控通信。
    - 需要留痕查看代理间通信记录（排错/审计）。
  - 推荐触发关键词：`局域网代理通信`、`UDP消息`、`发现其他agent`、`agent协作`、`消息日志`。

- Skill 名称：markdown-linter
  - 功能：扫描工作区 Markdown 文件，检查本地链接失效、引用缺失和基础结构问题。
  - 典型使用场景：
    - 更新 `README.md`、`MEMORY.md`、`SKILL.md` 后做一次文档完整性检查。
    - 项目迁移/重命名文件后，快速找出失效的相对路径链接。
    - 发布技能或文档前，做“链接是否都可用”的质量门禁。
  - 推荐触发关键词：`Markdown检查`、`文档死链检查`、`README自检`、`链接失效排查`、`发布前文档校验`。
