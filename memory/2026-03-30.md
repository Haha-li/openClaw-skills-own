- 2026-03-30: User created a dedicated Discord research channel (channel ID 1487965578353443056). OpenClaw Discord config was updated to allow that channel, set channel-level `systemPrompt` to research behavior, and set `requireMention: false` for that channel while keeping main channel mention-gated.
- 2026-03-30: Removed Grok-related config from `~/.openclaw/openclaw.json`: deleted `env.GROK_API_KEY`, `models.providers.grok`, and all `grok/...` entries from `agents.defaults.models`. Default model remains `openai-codex/gpt-5.4`.
- 2026-03-30: Investigated Discord image sending issue. Root cause appears to be current agent session lacking true `message` tool exposure; inline `MEDIA:...` was sent as plain text instead of rendered attachment. We removed `tools.profile: "coding"` and later added minimal `agents.defaults.tools.message` config in `~/.openclaw/openclaw.json`; gateway restart still required/was requested after final change to verify whether true media sending becomes available.
- 2026-03-30: For browser automation on this ARM64 server, direct local `agent-browser` runtime is not viable (`agent-browser install` unsupported on Linux ARM64; snap Chromium path unreliable). Working solution verified: keep `agent-browser` but connect it to Docker container `local-chrome` (`ghcr.io/browserless/chromium:latest`) via CDP endpoint `http://127.0.0.1:3000/`.
- 2026-03-30: Verified browserless container status: running for days, port 3000 exposed, `/json/version` returns Chrome 145 debugger info with websocket endpoint. `agent-browser` can successfully operate through `--cdp http://127.0.0.1:3000/`.
- 2026-03-30: Verified `agent-browser` via browserless on ARM64 can open normal sites (`example.com`, later `https://www.baidu.com/`) and read title/URL. `chatgpt.com` is blocked by Cloudflare challenge. Initial Baidu failures were transient/timeout-related, not fundamental tool failure.
- 2026-03-30: Added workspace docs capturing ARM64 browser strategy and usage: `docs/browser-strategy-arm64.md` and `docs/agent-browser-arm64-usage.md`.
- 2026-03-30: Updated `scripts/cleanup-cache.sh` so cache cleanup also deletes generated screenshot files under `/root/.openclaw/workspace/output/` (png/jpg/jpeg/webp).
